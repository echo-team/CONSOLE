var core = new Core(),
    button = new Button({core: core}),
    plugins = new Plugins({parent: document.head});

let plug =
    [
        {
            background: null,
            content: null,
            page: null
        }
    ];
